define([
    'require'
], function(require) {
    var DEBUG = {
        controller: true,
        spaceout: true,
        panes: true,
        view: {
            init: true,
            render: true
        }
    };

    return DEBUG;
});
