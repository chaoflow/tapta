define([
    'require'
], function(require) {
    var DEBUG = {
        controller: true,
        spaceout: true,
        view: {
            init: true,
            render: true
        }
    };

    return DEBUG;
});
